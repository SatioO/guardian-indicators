# Signing key — NEVER commit. Lives locally / in CI secrets only.
signing-key.private.pem
*.private.pem
*.key
# Local env files: this repo reads none, and they tend to collect secrets.
.env
.env.*
node_modules/
publish-report.md
